---
'mcp-watchtower': patch
---

Stream live per-tool scan progress and findings in the default human-readable CLI output, keep `--json` output machine-readable, and reserve `--verbose` for future expanded logging.
