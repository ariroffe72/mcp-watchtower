---
'mcp-watchtower': patch
---

Fix the published package contents so the CLI includes semantic embedding runtime files, and fail packaging checks when required runtime assets are missing from the npm tarball.
