# Internal pipeline scripts — not needed by end users
dist/scripts/
dist/src/crawler/
dist/src/embeddings/
dist/src/index-builder/

# Source data — bundled index lives in src/data/ and is included intentionally,
# but raw corpus/embeddings files must never ship
src/data/corpus.json
src/data/embeddings.json

# Dev/test artifacts
test-servers/
tests/
.env
*.env